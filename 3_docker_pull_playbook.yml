- name: Docker image pull and deploy
  hosts: localhost
  user: ubuntu
  become: yes

  tasks:
  - name: Docker image pull
    shell:
      cmd: docker pull amclau303/project:1

  - name: kubernetes deploy
    shell:
      cmd: kubectl create deployment kube-server --image=amclau303/project:1.0
